#include<stdio.h>
#include<math.h>
#include<string.h>
    sum(ara);

int main(){
    int n;
    scanf('%d', &n);
    int ara[100];
    for (int i = 1; i <= n; i++)
    {
        scanf("%d", ara[i]);
    }
    sum(ara);
    return 0;
}
int sum(int ara[]){
    
    return 0;
}
