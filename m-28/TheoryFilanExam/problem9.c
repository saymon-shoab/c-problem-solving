#include <stdio.h>
int main()
{
    int x = 10;
    b = 20, c;
    // a semantic error
    x + y = c;
    printf("%d", c);
    return 0;
}

#include <stdio.h>
int main()
{
    int array[5];
    // a runtime error
    printf("%d", array[10]);
    return 0;
}
